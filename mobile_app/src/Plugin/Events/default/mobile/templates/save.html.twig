{# ============================================================
 # templates/save.html.twig
 # Route: mobile_app.settings.save  /mobile/settings/save
 # JSON endpoint — consumed by engine.js saveSetting()
 # This template outputs a JSON response only.
 # The real logic is in ManifestController::save()
 # ============================================================ #}
{{ response|raw }}
