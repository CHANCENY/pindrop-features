{# File Field Template #}
<div class="field-group field-type-{{ field.struct_type }} {{ field.component|join('-') }}">
    {% if field.settings.label_display != 'hidden' %}
        <label for="{{ name }}" class="field-label">
            {{ field.label }}
            {% if field.required %}<span class="required">*</span>{% endif %}
        </label>
    {% endif %}
    
    <input 
        type="file"
        id="{{ name }}"
        name="{{ name }}{% if field.multiple %}[]{% endif %}"
        class="form-control file-field"
        {% if field.fileTypes %}accept=".{{ field.fileTypes|replace({',': ',.'})|replace({' ': ','}) }}"{% endif %}
        {% if field.multiple %}multiple{% endif %}
        {% if field.required %}required{% endif %}
        {% if field.cardinality and field.multiple %}data-cardinality="{{ field.cardinality }}"{% endif %}
    />
    
    {% if field.maxSize %}
        <div class="file-size-info">
            <small>Maximum file size: {{ field.maxSize }}MB</small>
        </div>
    {% endif %}
    
    {% if field.helpText %}
        <div class="field-help">{{ field.helpText }}</div>
    {% endif %}
    
    {% if field.comment %}
        <div class="field-comment">{{ field.comment }}</div>
    {% endif %}
</div>
