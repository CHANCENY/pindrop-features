<div class="drupal-button-display-settings">
    <div class="form-section">
        <h3>Display Settings</h3>
        <div class="form-grid">
            <div class="form-item full-width">
                <p>Button field display settings will be rendered here when viewing content.</p>
                <p>Example: <button class="btn btn-{{ field.config.buttonStyle|default('primary') }}">{{ field.config.buttonText|default('Click me') }}</button></p>
            </div>
        </div>
    </div>
</div>
